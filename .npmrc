# @tensorflow/tfjs-react-native@1.0.0 (last release 2021) declares stale peer
# dependencies (expo-camera ^13, expo-gl ^13, react-native-fs). We knowingly
# run it against Expo SDK 54 — see README "Compatibilité" — so peer conflicts
# must not fail the install.
legacy-peer-deps=true
